<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head', {title: null}); %>
</head>
<body>
    <%- include('../partials/header', {title : null}); %>
    <main>
        <form action="/report" method="post"><button>Generovat Report</button></form>
        <div class="report">
            <% if (data) { %>
                <h2>Report</h2>
                <p>Počet úřadů v Praze: <%=data[0]%></p>
                <p>Počet řidičů s ZTP: <%=data[1]%></p>
                <p>Počet vozidel s výkonem >50kw: <%=data[2]%></p>
            <% } %>
        </div>
    </main>
    <%- include('../partials/footer'); %>
    <script src="script.js"></script>
</body>
</html>